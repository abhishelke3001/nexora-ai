name: NEXORA Market Scanner

on:
  schedule:
    - cron: "*/5 * * * *"
  workflow_dispatch:

jobs:
  scan:
    runs-on: ubuntu-latest

    steps:
      - name: Run NEXORA scanner
        run: |
          curl -fsS "https://YOUR-VERCEL-DOMAIN.vercel.app/api/automation/scan"
